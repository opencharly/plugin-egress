# 2026.250.0951 — feat: egress schema for the crabbox local-container config (crabbox-yaml)

## Summary

Egress schema for the rendered crabbox local-container config (`~/.config/crabbox/config.yaml`) that the new `config:` plan-step verb produces (contract: opencharly/spec#117 + sdk emitter opencharly/sdk#237).

- `egress-schemas/egress_crabbox.cue`: `#CrabboxYaml` — types the docs/providers/local-container.md surface (non-empty `provider`; `localContainer.{runtime,image,user,workRoot,cpus,memory,network,dockerSocket,noHostname,volumes}` typed; `noHostname`/`dockerSocket` REAL bools). The `...` tolerances keep upstream field additions from hard-failing while still catching type errors.
- `main.go`: registers the kind (`"crabbox-yaml": "#CrabboxYaml"`).
- `crabbox_egress_test.go`: good minimal / good noHostname-bool / string-bool rejected / missing-provider rejected.

## How tested

```
$ go build ./...
$ go test ./... -run TestEgress -count=1 -v
--- PASS: TestEgressCrabboxYaml (0.02s)
--- PASS: TestEgressValidate (0.02s)
PASS
ok  github.com/opencharly/plugin-egress/candy/plugin-egress 0.040s
$ go test ./... -count=1
?   github.com/opencharly/plugin-egress/candy/plugin-egress/cmd/serve [no test files]
ok  github.com/opencharly/plugin-egress/candy/plugin-egress
```

## Rulebook compliance (per-rule dispositions)
- R1 (RCA every anomaly): N/A — no failure/anomaly observed; the schema mirrors the upstream local-container.md contract.
- R2 (finish the cutover): N/A — additive egress schema; no cutover in this repo.
- R3 (no duplication): the schema follows the existing package-less egress-schema pattern (concatenated def set + kindDefPaths) — no parallel mechanism (HOW: single registration in kindDefPaths).
- R4 (no workarounds): no sleeps/retries/bypasses (HOW: pure schema + registration).
- R5 (delete legacy): N/A — nothing removed.
- R6 (git safety): branch-per-change PR, no force-push (HOW: feat/crabbox-yaml-egress only).
- R7 (prove the gate): build + both egress tests pasted above (HOW: test run output).
- R8 (emitted artifacts): N/A — no Containerfile/artifact emission touched.
- R9 (binary equals source): N/A — this repo has no deployed binary (plugin compiled into consumers).
- R10 (fresh disposable proof): N/A for this repo alone — the end-to-end disposable proof lands in the distro-cachyos bed R10 (check-crabbox-e2e-pod) once the wave's charly re-pin merges; the schema's live execution is proven here by the egress test suite.

## Change classification

- **Class:** plugin-egress schema (contract consumer). **Verification gate:** egress test suite green. **Attribution:** fully tested and validated.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*